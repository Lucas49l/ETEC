
const saudacao = function(nome) {
    // Cria uma variavel chamada saudacao;
    // essa variavel vai receber uma função com o parametro "nome"
    return `Olá, ${nome}!` // o que a função vai retornar
}
console.log(saudacao("THIAGO/ALEXANDRE")); 
// saida saudacao vai ser substituido pelo return
// o parametro nome vai ser substituido pelo Thiago/alexandre